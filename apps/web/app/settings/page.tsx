export default function Page() {
  return (
    <div className="max-w-2xl">
      <h1 className="text-xl font-semibold">Cài đặt</h1>
      <p className="text-[13px] text-muted mt-1">Thuộc chặng <b>M2+</b> — chưa triển khai.</p>
      <div className="card mt-5 text-[12.5px] text-muted">
        <p>API key, cấu hình GPU, giới hạn theo nền tảng, proxy và thông báo.</p>
        <p className="mt-2">
          Xem chi tiết task trong <code className="font-mono">docs/03-BACKLOG-CONG-VIEC.md</code>.
        </p>
      </div>
    </div>
  );
}
